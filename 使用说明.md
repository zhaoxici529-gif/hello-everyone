# 自媒体 AI 工作台 · 使用说明

写给「心身同调」的学员看的，不需要任何技术背景。

## 一、安装

**Windows**

1. 双击 `自媒体 AI 工作台_0.1.0_x64-setup.exe`（推荐），一路「下一步」即可；
   也可以装 `.msi` 版本，效果一样。
2. 装完在**开始菜单**里搜索「自媒体 AI 工作台」打开。
3. 如果弹出蓝色的「Windows 保护了你的电脑」提示，点 **更多信息 → 仍要运行**。
   这是 Windows 对没有购买代码签名证书的软件的统一提示，不是病毒。

**macOS**

1. 打开 `自媒体 AI 工作台_0.1.0_aarch64.dmg`（Intel 芯片用 x64 那个）。
2. 把图标拖进「应用程序」文件夹。
3. 第一次打开如果提示「无法验证开发者」：在「应用程序」里**右键点图标 → 打开 → 再点打开**，
   之后就能正常双击了。

## 二、第一次打开：三步引导

1. **欢迎页** —— 点「开始设置」。
2. **填 API Key** —— 可以粘贴自己的 Key，也可以点「先用免费体验额度」跳过（有 3 次免费）。
3. **建第一个人物小传** —— 填个名字就行，比如「小林」，其余之后慢慢补。

三步走完就进入工作台了。这些设置随时能在「设置」和「人物小传」里改。

## 三、怎么获取 API Key（以 DeepSeek 为例）

1. 打开 <https://platform.deepseek.com/api_keys>，用手机号注册、登录。
2. 左侧菜单点 **API Keys** → **创建 API Key** → 点复制。
   注意：这串字符**只在创建时显示一次**，关掉就看不到了，先粘到记事本里。
3. 回到工作台「设置」页 → 选 DeepSeek → 把 Key 粘贴到输入框 → 点**保存** → 点**测试连接**。
   看到「连接成功」就说明可以用了。

其他四家（通义千问、豆包、Kimi、智谱 GLM）流程一样，设置页里每个模型旁边都有直达官网的按钮，
点开「怎么注册、怎么拿 Key、怎么填？（图文教程）」还有带图的步骤。

**关于钱**：这些 API 是按用量计费的，DeepSeek、Kimi 等注册后会送一些免费额度，
按新学员一天写两三篇的量，几块钱能用很久。API Key 只存在你自己电脑上，是加密保存的。

## 四、日常怎么用

顶部有 7 个菜单，建议按这个顺序上手：

| 菜单 | 做什么 |
| --- | --- |
| **人物小传** | 先写「我自己」：性格、口头禅、受众、禁忌。写文案前定好这个，AI 写出来才像你 |
| **素材库** | 把图片、视频、音频、文字拖进来，会自动生成缩略图，可以打标签 |
| **创作** | 主战场：中间是这次的内容，周围是「找资料 / 起标题 / 写文案 / 做封面 / 学习改稿 / 发布与复盘」 |
| **文案管理** | 草稿箱：改稿、切状态（草稿→待发→已发）、版本历史、导出 txt / markdown |
| **发布与经营** | 登记已发布作品，按月份看，填成交人数、客资量、引流私域人数、咨询人数 |
| **首页** | 每天打开先看这里：今日主线、待办、番茄钟、随手记录 |
| **计划 / 今日热点 / 内容流程** | 还在建设中，先留着位置 |

一个典型的写作流程：**人物小传 → 创作页「写文案」→ 选文体 → 生成 3 版 → 对话式修改（如「再口语化一点」）→ 保存到文案库 → 文案管理里改定稿 → 标记为已发布。**

## 五、数据存在哪 / 怎么备份

- 所有数据都在你自己电脑上：`%APPDATA%\自媒体AI工作台\`（素材文件在里面的「素材库」文件夹）。
- 备份：打开**记忆库**（首页常用助手里的「知识库维护管家」）→ 点**导出备份**，会下载一个 JSON 文件。
- 恢复：同一个页面点**导入恢复**，选择之前导出的 JSON。
- 卸载软件不会删除数据；要彻底清空，手动删掉上面那个文件夹即可。

## 六、常见问题

**打开是白屏？**
请用安装包装的版本。项目目录里 `src-tauri\target\debug` 下的那个 exe 是开发版，单独运行会白屏。

**提示 API Key 无效？**
多半是复制时带了空格，或者复制不完整。回官网重新复制一次，粘贴时注意首尾不要有空格。

**提示免费体验额度已用完？**
在设置页填一个自己的 API Key 就能继续，不再限次数。

**网络错误 / 连不上？**
检查网络；如果你开了代理软件，确认它没有拦截；等几十秒再试。

**长得太长滚不动？**
用鼠标滚轮或右侧滚动条，页面内容在中间区域滚动，顶部导航固定不动。

**想要 mac 安装包？**
mac 的 `.dmg` 必须在 mac 电脑上打包：在项目目录执行 `pnpm tauri build --bundles dmg`。
Windows 上只能打出 Windows 安装包，这是系统限制，不是配置问题。